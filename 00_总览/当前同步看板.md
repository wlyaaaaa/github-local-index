# 当前同步看板

更新时间：2026-07-09

| 项目 | 可见性 | 当前状态 | 决策 |
| --- | --- | --- | --- |
| 仓库总数 | - | 28 个 GitHub 仓库，26 个已发现本地 clone，2 个未发现 clone | 持续刷新 |
| 未推送队列 | - | 0 个需处理项 | 无需处理 |
| 项目完善度 | 公开索引 | 仓库干净，单测通过，稳定一致性漂移为 0；只剩计划任务运行时间类易变漂移 | 进入长期维护态 |
| 计划任务治理 | 本机自动化 | 坏路径、调度器拒绝运行和后台窗口策略已收口；`0xC000013A` 按人为中断/关机注销优先观察 | 定期检查，不继续大改 |
| PCConfig v0.1 | 本机配置 | 机器配置、路径事实、迁移门禁和第一批 7 张项目卡已验收；GitHub 项目迁移 Phase 1 已完成 11 个低依赖仓库归位 | Phase 2 只处理服务/计划任务/路径依赖项目 |
| Codex 默认联动 | 规则 | 实际修改任意 Git 工作区后，默认提交/推送目标仓库并同步本索引 | 用户明确要求只本地、不提交或不推送时跳过 |

## 下一步优先级

1. 定期运行 `tools\Test-GitHubLocalIndexConsistency.ps1 -SkipFetch`；稳定漂移为 0 时不为了计划任务时间戳自动提交。
2. Codex 实际修改任意 Git 工作区后，默认同步目标仓库，再同步本索引。
3. 对未推送队列中的公开仓库先做暴露面审查。
4. 对未发现 clone 的仓库决定是否 clone 到固定目录或标记远端存档。
5. 若私有仓库可见性发生变化，立即重新审计密钥备份策略。
6. PCConfig 已达到 v0.1；不要为视觉完整继续扩项目卡。
7. GitHub-indexed 项目迁移下一步是 Phase 2：移动前必须同步更新计划任务、启动脚本和绝对路径引用。
