# 云端备份状态

更新时间：2026-07-08 10:59:00 中国时间 (UTC+8)

本文件只记录公开安全摘要：目录名、大小、文件数、任务状态和处理结论。不记录 token、密钥值、完整日志、完整计划任务 XML 或具体私有文件内容。

## Google Drive / rclone

远端：已配置并可访问。实际远端名在本机为邮箱形式，公开文档不写入账号值；脚本会自动探测第一个 rclone 远端。

| Drive 路径 | 当前状态 | 大小 / 文件数 | 结论 |
| --- | --- | --- | --- |
| `Backups/WLY` | 已存在 | 1.37 GB / 3 个对象 | DevConfig 配置包已上云，包含 2 个日期包 + `latest.zip`。 |
| `Backups/WeChat/xwechat_files` | 续传中 | 24.59 GB / 21567 个对象，约 64.8% | 微信完整聊天记录尚未完成；完整历史增量续传正在运行，rclone 会跳过已上传文件继续补齐。后续计划任务默认单次 Drive 上传有 8G 流量保险丝。 |
| `Backups/WeChat/_KEYS` | 已存在 | 3 个小文件 | 微信恢复密钥小文件已上云；公开索引不记录内容。 |
| `Backups/AI大模型` | 已存在 | 0.39 GB / 1883 个对象 | AI 大模型相关轻量备份目录已上云。 |

## GitHub 备份/同步仓库

本次抽查的关键备份仓库均为本地工作区干净、与远端同步：

| 仓库 | 本地路径 | 状态 |
| --- | --- | --- |
| `wlyaaaaa/devconfig-backup` | `E:\DevConfigBackup` | `main` 已推送，当前提交 `8b1ad30`；公开仓库只含脚本/文档，不含备份数据。 |
| `wlyaaaaa/github-local-index` | `E:\GitHub总索引` | 本次刷新后待提交本文件和生成摘要。 |
| `wlyaaaaa/openclaw-backup` | `E:\OpenClawBackup` | 已同步。 |
| `wlyaaaaa/codex-memory` | `E:\CodexMemoryBackup` | 已同步。 |
| `wlyaaaaa/gemini-memory` | `E:\GeminiMemoryBackup` | 已同步。 |
| `wlyaaaaa/claude-memory` | `E:\ClaudeMemoryBackup` | 已同步。 |
| `wlyaaaaa/HealthLongevity` | `E:\HealthLongevity` | 已同步。 |
| `wlyaaaaa/Scripts` | `E:\Scripts` | 已同步。 |

## 计划任务结论

已用管理员权限重建 `E:\DevConfigBackup` 的备份任务，实际任务现在与脚本一致：

| 任务 | 目标 | 当前结论 |
| --- | --- | --- |
| `DevConfigBackup-Local` | 本地配置包 | 已重建，下一次运行会产生新的本地恢复点。 |
| `DevConfigBackup-Weekly` | U盘 + Drive 配置包 | 已重建，每周晚间执行，Drive 保留日期包和 `latest.zip`。 |
| `DevConfigBackup-OnUSB` | 插入 U 盘触发 | 已重建。 |
| `WeChatBackup-Weekly` | U盘 + Drive 微信完整聊天记录 | 已重建为完整历史增量；不再默认只传 `db_storage`。 |

同时抽查的其他备份/同步任务：

- `AIModelsBackup-*`：最近返回码 0。
- `AutoDigitalBackupToH`：最近返回码 0。
- `Codex Memory Backup` / `Gemini Memory Backup` / `OpenClaw Memory Backup` / `OpenClawGateway AutoPush`：最近返回码 0。
- `HealthLongevity Private Backup`：最近返回码 0。
- `Scripts_AutoPush`：最近返回码 0。
- `TimeAudit_DailyBackup`：最近返回码 0。

当前仍需观察：

- `Backups/WeChat/xwechat_files` 仍在补齐完整历史；完成前不要把 Drive 端微信历史视为完整。
- `SunshineRemote-VerifyPath-Daily` 和 `TimeAudit_AutoStart` 有非零返回码，但不属于本次 GitHub/Drive 备份链路；保留在计划任务异常清单继续观察。
