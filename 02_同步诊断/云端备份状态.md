# 云端备份状态

更新时间：2026-07-23 12:34:22 中国时间 (UTC+8)

本文件只记录公开安全摘要：目录名、大小、文件数、任务状态和处理结论。不记录 token、密钥值、完整日志、完整计划任务 XML 或具体私有文件内容。

## Google Drive / rclone

远端：已配置并可访问。实际远端名在本机为邮箱形式，公开文档不写入账号值；脚本会自动探测第一个 rclone 远端。

| Drive 路径 | 当前状态 | 大小 / 文件数 | 结论 |
| --- | --- | --- | --- |
| `Backups/WLY` | 已存在 | 1.37 GB / 3 个对象 | DevConfig 配置包已上云，包含 2 个日期包 + `latest.zip`。 |
| `Backups/WeChat/xwechat_files` | 内容校验通过 | 129,051 个匹配文件；本次增量上传 1.353 GiB / 696 个变化文件 | `rclone copy --checksum` 仅上传新增或变化文件，随后 `rclone check` 返回 0 differences；不是 39.88 GB 源目录的全量重传。后续计划任务仍保留单次 8G 流量保险丝。 |
| `Backups/WeChat/_KEYS` | 已存在 | 3 个小文件 | 微信恢复密钥小文件已上云；公开索引不记录内容。 |
| `Backups/AI大模型` | 已存在 | 0.39 GB / 1883 个对象 | AI 大模型相关轻量备份目录已上云。 |

## GitHub 备份/同步仓库

本次抽查的关键备份仓库均为本地工作区干净、与远端同步：

| 仓库 | 本地路径 | 状态 |
| --- | --- | --- |
| `wlyaaaaa/devconfig-backup` | `E:\Projects\Backups\devconfig-backup` | 已把计划任务主目标迁到 `G:\80_Backup` 热备；H 仅保留人工冷备入口。公开仓库只含脚本/文档，不含备份数据；本轮改动已推送。 |
| `wlyaaaaa/github-local-index` | `E:\GitHub总索引` | 本次刷新后待提交本文件和生成摘要。 |
| `wlyaaaaa/openclaw-backup` | `E:\Projects\Backups\openclaw-backup` | 已同步。 |
| `wlyaaaaa/codex-memory` | `E:\Projects\Backups\codex-memory` | 已同步。 |
| `wlyaaaaa/gemini-memory` | `E:\Projects\Backups\gemini-memory` | 已同步。 |
| `wlyaaaaa/claude-memory` | `E:\Projects\Backups\claude-memory` | 已同步。 |
| `wlyaaaaa/Scripts` | `E:\Projects\Tools\Scripts` | 软件环境计划任务已改为 `AutoDigitalBackupToG`，下载归档也只提供 G 热备脚本；项目不再保留直写 H 的入口。本轮改动已推送。 |

旧 Health 私有仓库已经退役，不再属于活动 GitHub 仓库或同步链；其历史恢复材料已由 PersonalOS 的加密恢复链承接。公开索引只保留这一退役结论，不记录私有对象标识、恢复证明或介质细节。

## 计划任务结论

已用管理员权限重建 `E:\Projects\Backups\devconfig-backup` 的备份任务，实际任务现在与脚本一致：

| 任务 | 目标 | 当前结论 |
| --- | --- | --- |
| `DevConfigBackup-Local` | 本地配置包 | 已实际运行，返回码 0；默认包已排除 Codex session 历史与大日志数据库。 |
| `DevConfigBackup-Weekly` | G盘热备 + Drive 配置包 | 已实际运行 `Hot,Drive`，返回码 0。 |
| `WeChatBackup-Weekly` | G盘热备 + Drive 微信完整聊天记录 | 已实际运行 `Hot,Drive`，增量上传和内容校验均返回 0；不自动写 H。 |

同时抽查的其他备份/同步任务：

- `AIModelsBackup-Hot-Daily` / `AIModelsBackup-Drive-Daily`：已重建；H 的 USB-Daily / OnUSB 任务已删除。
- `AgentsHotMirror-Daily`：已建立每日 G 盘镜像，保留 `E:\.agents` 当前工作树；H 不参与自动链路。
- `AutoDigitalBackupToG`：已重建；旧 `AutoDigitalBackupToH` 已删除。
- `Codex Memory Backup` / `Gemini Memory Backup` / `OpenClaw Memory Backup` / `OpenClawGateway AutoPush`：最近返回码 0。
- `Scripts_AutoPush`：最近返回码 0。
- `TimeAudit_DailyBackup`：最近返回码 0。

旧 Health 私有同步任务已经退役，不再属于活动计划任务。

本轮备份链路没有待观察项。其他非备份计划任务的告警仍保留在计划任务异常清单，由各自 owner 处理。


## 监控任务

- `WeChatDrive-Monitor-Hourly` 当前为 Disabled；正常运行只依赖每周增量任务。
