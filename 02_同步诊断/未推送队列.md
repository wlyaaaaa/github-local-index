# 未推送队列

更新时间：2026-07-26

## 当前队列

| 仓库 | 可见性 | 状态 | 队列原因 | 决策 |
| --- | --- | --- | --- | --- |
| wlyaaaaa/.agents | PRIVATE | `codex/personalos-beacon-receipt` 已同步，`0/0`（cached）<br>`main` 已同步，`0/0`，脏工作区 10 项（cached）<br>`codex/local-gpu-arbitration` 已同步，`0/0`（cached）<br>`codex/s8-wp4-personalos-router` 无 upstream（cached）<br>`codex/s9-beta-total-deadline` 无 upstream（cached）<br>`codex/s9-health-route-doc` 无 upstream（cached）<br>`codex/s9-wp4-release` 无 upstream（cached） | 无 upstream；脏工作区 10 项；cached 远端引用 | 私有仓库按备份需求确认后提交<br>需人工确认 upstream 后再判断推送策略<br>正常维护 |
| wlyaaaaa/ai-cli-profile-manager | PUBLIC | `main` 已同步，`0/0`，脏工作区 2 项（cached）<br>`codex/public-observer-events` ahead/behind `0/1`（cached） | 脏工作区 2 项；behind 1；cached 远端引用 | 公开仓库先做暴露面审查，再按显式路径提交<br>先同步远端变更，再决定是否提交或推送 |
| wlyaaaaa/codex-local-remote | PUBLIC | `main` 已同步，`0/0`，脏工作区 64 项（cached）<br>`detached` 无 upstream（cached） | 无 upstream；脏工作区 64 项；cached 远端引用；detached worktree | 公开仓库先做暴露面审查，再按显式路径提交<br>需人工确认 upstream 后再判断推送策略 |
| wlyaaaaa/github-local-index | PUBLIC | `main` 已同步，`0/0`，脏工作区 11 项（cached）<br>`codex/s9-wp4-owner-closeout` 无 upstream（cached）<br>`codex/s9-wp4-release` 无 upstream（cached） | 无 upstream；脏工作区 11 项；cached 远端引用 | 公开仓库先做暴露面审查，再按显式路径提交<br>需人工确认 upstream 后再判断推送策略 |
| wlyaaaaa/legal-filing-kit | PRIVATE | `codex/v2-s5-ai-facade` 已同步，`0/0`，脏工作区 3 项（cached）<br>历史审计快照固定于 `87dcfd1`，远端领先 2，不自动同步（cached）<br>历史审计快照固定于 `859b083`，远端领先 1，不自动同步（cached）<br>历史审计快照固定于 `25529a0`，远端领先 5，不自动同步（cached） | 脏工作区 3 项；cached 远端引用 | 保持提交固定审计快照；需要新证据时创建新审计副本<br>私有仓库按备份需求确认后提交 |
| wlyaaaaa/llm-backend-toolkit | PUBLIC | `main` 已同步，`0/0`（cached）<br>历史审计快照固定于 `25d2794`，detached 且无 upstream，不自动同步（cached）<br>历史审计快照固定于 `4bd5081`，detached 且无 upstream，不自动同步（cached）<br>历史审计快照固定于 `df3f17d`，detached 且无 upstream，不自动同步（cached）<br>`codex/explicit-cache-key-identity` 无 upstream（cached）<br>`codex/job-input-integrity` 无 upstream（cached）<br>`codex/model-observer-gui` ahead/behind `0/1`（cached） | 无 upstream；behind 1；cached 远端引用 | 保持提交固定审计快照；需要新证据时创建新审计副本<br>先同步远端变更，再决定是否提交或推送<br>需人工确认 upstream 后再判断推送策略<br>正常维护 |
| wlyaaaaa/PCConfig | PRIVATE | `codex/local-gpu-arbitration` 已同步，`0/0`（cached）<br>`codex/s8-wp4-personalos-runtime` 无 upstream（cached）<br>`main` 已同步，`0/0`，脏工作区 31 项（cached）<br>`codex/personalos-m1-runtime-upgrade` ahead/behind `2/10`（cached）<br>`codex/s9-nearline-fix` 无 upstream（cached）<br>`codex/s9-wp4-owner-closeout` 无 upstream（cached）<br>`codex/s9-wp4-release` 无 upstream（cached） | 无 upstream；脏工作区 31 项；ahead 2；behind 10；cached 远端引用 | 私有仓库按备份需求确认后提交<br>先同步远端变更，再决定是否提交或推送<br>需人工确认 upstream 后再判断推送策略<br>正常维护 |
| wlyaaaaa/wechat-history-ai-bridge | PUBLIC | `master` 已同步，`0/0`（cached）<br>`codex/weflowbridge-ai-integration-1-0` 已同步，`0/0`（cached）<br>`codex/weflowbridge-ai-readme-v2` 无 upstream（cached）<br>`codex/weflowbridge-star-readme` 已同步，`0/0`（cached）<br>`codex/wechat-history-ai-bridge-branding` 已同步，`0/0`（cached） | 无 upstream；cached 远端引用 | 需人工确认 upstream 后再判断推送策略<br>正常维护 |
