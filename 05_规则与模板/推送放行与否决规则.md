# 推送放行与否决规则

## 放行

| 条件 | 处理 |
|---|---|
| GitHub 可见性为 `PRIVATE`，且是备份/恢复/个人知识库 | 放行推送，包含密钥或私有配置也不默认脱敏 |
| GitHub 可见性为 `PUBLIC`，内容为文档/代码/清洗摘要且无敏感值 | 放行提交和推送 |
| 当前仓库工作区干净，仅 ahead | 可直接推送 |

## 否决

| 条件 | 处理 |
|---|---|
| 公开仓库含真实密钥值、私钥内容、token 值 | 否决原样提交 |
| 公开仓库含未脱敏截图、原始日志、进程列表、绝对路径密集泄露 | 否决原样提交 |
| 工作区混合临时产物、重复副本、生成帧和代码变更 | 否决 `git add .`，要求整理后复审 |

## 关键命令

```powershell
gh repo view OWNER/REPO --json visibility
git status -sb
git log --oneline --decorate --branches --not --remotes=origin
git push origin <branch>
```
